import Head from 'next/head'
import Image from 'next/image'
import styles from '../../styles/homepage.module.css'
import { useRouter } from "next/router"
import { useState } from 'react'
import { setTimeout } from 'react'
export default function HomePage() {
  const router = useRouter()

  const [numPessoas, setNumPessoas] = useState(12)


  const pessoas = setInterval(function() {
    if (Math.ceil(Math.random()*10) > 6) {setNumPessoas(numPessoas - 1) }
    else {setNumPessoas(numPessoas + 1)};
    }, 10000);

    clearInterval(numPessoas)




  return (
    <div className={styles.geral}>
      <Head>
        <title>Mercearia da Bianca</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/mini.png" />
      </Head>
      <div className={styles.logoCarro}><img width="280px" height="90px" src="../logoOficial.png" /></div>
      <hr className={styles.hr}></hr>

      <div className={styles.baixo}>
        <div className={styles.catalgo} onClick={() => router.push("/brochure")}>
          <p className={styles.textoCatalogo}>CATÁLOGO</p>
        </div>
        <div className={styles.jogo} onClick={() => router.push("/prizes")}>
          <p className={styles.textoPremios}>GANHE HOJE PRÉMIOS</p>
        </div>
      </div>
      <div className={styles.cima}>
        <div className={styles.tempoEspera}>
          <span>Tempo de espera</span>
          <p className={styles.tempoMin}>{Math.ceil(numPessoas * 1.5)} minutos</p>
        </div>
        <div className={styles.pessoas}>
          <span>Fila de espera</span>
          <p className={styles.quantPessoas}>{numPessoas} pessoas</p>
        </div>
      </div>

    </div>


  )
}
